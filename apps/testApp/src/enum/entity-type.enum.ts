'use strict';

export enum EntityTypeEnum {
  user = 'user',
  idea = 'idea',
  comment = 'comment',
  challenge = 'challenge',
  opportunityType = 'opportunity_type',
  stage = 'stage',
  bookmarkedView = 'bookmarked_view',
}
